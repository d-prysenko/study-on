{% extends 'base.html.twig' %}

{% block title %}Course{% endblock %}

{% block body %}
    <h1>Курс "{{ course.name }}"</h1>
    <div>
        {{ course.description }}
    </div><br>

    {% for lesson in lessons %}
        <div><a href="{{ path('lesson_show', {'id': lesson.id}) }}">{{ loop.index }}. {{ lesson.title }}</a></div>
    {% endfor %}

    <br>
    <a href="{{ path('course_index') }}">Назад</a>
    <a href="{{ path('lesson_new', {'course': course.id}) }}">Добавить урок</a>
    <a href="{{ path('course_edit', {'id': course.id}) }}">Редактировать этот курс</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
